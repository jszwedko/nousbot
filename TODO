add channel flood limiting, bot command use limiting
add administrative functions including kicks, bans, etc
plugin loading/unloading, general ACL work
decouple redis from memory, make it a config declaration rather than default
use in-memory store for default storage when redis isn't available
add logging support
